{
  "name": "pg-express-books",
  "private": true,
  "version": "1.0.0",
  "main": "server.js",
  "repository": "git@github.com:Vincent440/pg-express-books.git",
  "author": "vince s <vinceshury@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.21.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "pg": "^8.5.1"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "nodemon": "^2.0.6"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently \"nodemon --ignore 'client/*'\" \"yarn client\"",
    "client": "cd client && yarn start",
    "install": "cd client && yarn",
    "build": "cd client && yarn build",
    "heroku-postbuild": "yarn build",
    "test": "echo 'Error: Write tests for server' && exit 1"
  },
  "engines": {
    "node": "*",
    "yarn": "*"
  }
}
